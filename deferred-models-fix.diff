[1mdiff --git a/src/app.js b/src/app.js[m
[1mindex 6682547..cf28f86 100644[m
[1m--- a/src/app.js[m
[1m+++ b/src/app.js[m
[36m@@ -29,24 +29,46 @@[m [mexport function createApp({ store, env = process.env, oauthClient = null, fetchI[m
     catch (error) { console.error('Scheduler tick failed', error); return res.status(500).json({ error: 'scheduler_failed' }); }[m
   });[m
 [m
[32m+[m[32m  async function editDeferredResponse(interaction, response) {[m
[32m+[m[32m    const applicationId = env.DISCORD_APPLICATION_ID;[m
[32m+[m[32m    if (!applicationId || !interaction.token) throw new Error('Discord interaction webhook configuration is missing');[m
[32m+[m[32m    const webhookUrl = `https://discord.com/api/v10/webhooks/${applicationId}/${interaction.token}/messages/@original`;[m
[32m+[m[32m    const webhookResponse = await fetchImpl(webhookUrl, {[m
[32m+[m[32m      method: 'PATCH',[m
[32m+[m[32m      headers: { 'content-type': 'application/json' },[m
[32m+[m[32m      body: JSON.stringify(response?.data ?? { content: 'Something went wrong while handling that command.', flags: 64 })[m
[32m+[m[32m    });[m
[32m+[m[32m    if (!webhookResponse.ok) throw new Error(`Discord interaction webhook edit failed: ${webhookResponse.status}`);[m
[32m+[m[32m  }[m
[32m+[m
   app.post('/interactions', async (req, res) => {[m
     const valid = verifyDiscordRequest(req.rawBody, req.header('x-signature-ed25519'), req.header('x-signature-timestamp'), env.DISCORD_PUBLIC_KEY);[m
     if (!valid) return res.status(401).send('invalid request signature');[m
     if (req.body?.type === 1) return res.json({ type: 1 });[m
[31m-    if (req.body?.type === 3) {[m
[31m-      const customId = String(req.body.data?.custom_id ?? '');[m
[31m-      const [prefix, rating] = customId.split(':');[m
[31m-      if (prefix === 'feedback' && (rating === 'helpful' || rating === 'not_helpful')) {[m
[31m-        await store.recordFeedback(req.body.member?.user?.id ?? req.body.user?.id, req.body.message?.id, rating);[m
[31m-        return res.json({ type: 4, data: { content: 'Thanks for the feedback.', flags: 64 } });[m
[31m-      }[m
[31m-    }[m
[32m+[m
[32m+[m[32m    // Discord requires the initial interaction acknowledgement within roughly three seconds.[m
[32m+[m[32m    // Send the defer before any database, provider, or other external work.[m
[32m+[m[32m    res.json({ type: 5 });[m
[32m+[m
     try {[m
[31m-      const response = await handleInteraction(req.body, { store, env, fetchImpl, gmailAdapterFactory, summarizerFactory });[m
[31m-      return res.json(response);[m
[32m+[m[32m      let response;[m
[32m+[m[32m      if (req.body?.type === 3) {[m
[32m+[m[32m        const customId = String(req.body.data?.custom_id ?? '');[m
[32m+[m[32m        const [prefix, rating] = customId.split(':');[m
[32m+[m[32m        if (prefix === 'feedback' && (rating === 'helpful' || rating === 'not_helpful')) {[m
[32m+[m[32m          await store.recordFeedback(req.body.member?.user?.id ?? req.body.user?.id, req.body.message?.id, rating);[m
[32m+[m[32m          response = { type: 4, data: { content: 'Thanks for the feedback.', flags: 64 } };[m
[32m+[m[32m        }[m
[32m+[m[32m      }[m
[32m+[m[32m      if (!response) response = await handleInteraction(req.body, { store, env, fetchImpl, gmailAdapterFactory, summarizerFactory });[m
[32m+[m[32m      await editDeferredResponse(req.body, response);[m
     } catch (error) {[m
       console.error('Interaction failed', error);[m
[31m-      return res.status(500).json({ type: 4, data: { content: 'Something went wrong while handling that command.', flags: 64 } });[m
[32m+[m[32m      try {[m
[32m+[m[32m        await editDeferredResponse(req.body, { type: 4, data: { content: 'Something went wrong while handling that command.', flags: 64 } });[m
[32m+[m[32m      } catch (editError) {[m
[32m+[m[32m        console.error('Interaction error response failed', editError);[m
[32m+[m[32m      }[m
     }[m
   });[m
   return app;[m
[1mdiff --git a/src/discord/commands.js b/src/discord/commands.js[m
[1mindex 92ffd66..56a59a0 100644[m
[1m--- a/src/discord/commands.js[m
[1m+++ b/src/discord/commands.js[m
[36m@@ -29,22 +29,9 @@[m [masync function modelListing(discordUserId, deps) {[m
   const credentials = await deps.store.listAiCredentials(discordUserId);[m
   const components = [];[m
   const sections = [];[m
[31m-  const refreshCutoff = Date.now() - 24 * 60 * 60 * 1000;[m
[32m+[m[32m  // /models is intentionally cache-first. Provider refreshes are out of this request path.[m
   for (const credential of credentials) {[m
[31m-    let current = credential;[m
[31m-    const validatedAt = credential.validatedAt ? new Date(credential.validatedAt).getTime() : 0;[m
[31m-    if (!validatedAt || validatedAt < refreshCutoff) {[m
[31m-      try {[m
[31m-        const apiKey = decryptSecret(credential.encryptedApiKey, deps.env.SESSION_SECRET);[m
[31m-        const refreshed = await fetchProviderModels({ provider: credential.provider, baseUrl: credential.baseUrl, apiKey, fetchImpl: deps.fetchImpl });[m
[31m-        await deps.store.refreshAiCredentialModels(discordUserId, credential.id, refreshed.models);[m
[31m-        current = { ...credential, cachedModels: refreshed.models };[m
[31m-      } catch (error) {[m
[31m-        // A stale-cache refresh is best effort. Existing cached models remain usable.[m
[31m-        if (!credential.cachedModels?.length) sections.push(`${credentialDisplayName(credential)}: model refresh failed; no cached models are available.`);[m
[31m-      }[m
[31m-    }[m
[31m-    const models = current.cachedModels ?? [];[m
[32m+[m[32m    const models = credential.cachedModels ?? [];[m
     const label = credential.label ? `${credentialDisplayName(credential)} (${credential.label})` : credentialDisplayName(credential);[m
     sections.push(`${label}${credential.active ? ' [active]' : ''} — ${models.length} cached model${models.length === 1 ? '' : 's'}`);[m
     const buttons = [];[m
[1mdiff --git a/tests/byok.test.js b/tests/byok.test.js[m
[1mindex a3ac9a8..2a75084 100644[m
[1m--- a/tests/byok.test.js[m
[1m+++ b/tests/byok.test.js[m
[36m@@ -51,19 +51,24 @@[m [mtest('multiple provider keys are stored separately with cached models and encryp[m
   assert.deepEqual(store.credentials[1].cachedModels, [{ id: 'router-a' }, { id: 'router-b' }]);[m
 });[m
 [m
[31m-test('/models refreshes stale caches and falls back to existing data on refresh failure', async () => {[m
[32m+[m[32mtest('/models returns stale cached models without refreshing the provider', async () => {[m
   const store = makeFakeStore();[m
   store.credentials.push({ id: 7, provider: 'openai', label: null, baseUrl: 'https://api.openai.com/v1', encryptedApiKey: encryptSecret('sk-refresh-123456', env.SESSION_SECRET), cachedModels: [{ id: 'old-model' }], validatedAt: new Date(Date.now() - 48 * 60 * 60 * 1000) });[m
[31m-  const { fetchImpl, calls } = mockFetch({ 'https://api.openai.com/v1/models': [{ id: 'new-model' }] });[m
[31m-  const refreshed = await handleInteraction(command('u', 'models'), { store, env, fetchImpl });[m
[31m-  assert.equal(calls.some((call) => call.url.endsWith('/models')), true);[m
[31m-  assert.match(refreshed.data.content, /1 cached model/);[m
[31m-  assert.equal(store.credentials[0].cachedModels[0].id, 'new-model');[m
[31m-[m
[31m-  store.credentials[0].validatedAt = new Date(Date.now() - 48 * 60 * 60 * 1000);[m
[31m-  const failed = await handleInteraction(command('u', 'models'), { store, env, fetchImpl: async () => { throw new Error('provider timeout'); } });[m
[31m-  assert.match(failed.data.content, /1 cached model/);[m
[31m-  assert.equal(store.credentials[0].cachedModels[0].id, 'new-model');[m
[32m+[m[32m  let providerCalls = 0;[m
[32m+[m[32m  const listing = await handleInteraction(command('u', 'models'), {[m
[32m+[m[32m    store,[m
[32m+[m[32m    env,[m
[32m+[m[32m    fetchImpl: async () => {[m
[32m+[m[32m      providerCalls += 1;[m
[32m+[m[32m      await new Promise((resolve) => setTimeout(resolve, 150));[m
[32m+[m[32m      throw new Error('provider timeout');[m
[32m+[m[32m    }[m
[32m+[m[32m  });[m
[32m+[m[32m  assert.equal(providerCalls, 0);[m
[32m+[m[32m  assert.match(listing.data.content, /1 cached model/);[m
[32m+[m[32m  const modelButton = listing.data.components.flatMap((row) => row.components).find((button) => button.label === 'old-model');[m
[32m+[m[32m  assert.ok(modelButton);[m
[32m+[m[32m  assert.equal(store.credentials[0].cachedModels[0].id, 'old-model');[m
 });[m
 [m
 test('model selection switches the active model across providers and removal is scoped', async () => {[m
[1mdiff --git a/tests/mvp.test.js b/tests/mvp.test.js[m
[1mindex a395e69..2954525 100644[m
[1m--- a/tests/mvp.test.js[m
[1m+++ b/tests/mvp.test.js[m
[36m@@ -28,7 +28,7 @@[m [mfunction appWithKeyPair() {[m
   const keyPair = nacl.sign.keyPair();[m
   const db = openDatabase();[m
   const store = makeStore(db);[m
[31m-  const testEnv = { ...env, DISCORD_PUBLIC_KEY: Buffer.from(keyPair.publicKey).toString('hex') };[m
[32m+[m[32m  const testEnv = { ...env, DISCORD_PUBLIC_KEY: Buffer.from(keyPair.publicKey).toString('hex'), DISCORD_APPLICATION_ID: 'test-application' };[m
   const fetchImpl = async () => ({ ok: true, status: 200, json: async () => ({ data: [{ id: 'gpt-test', name: 'gpt-test' }] }) });[m
   return { app: createApp({ store, env: testEnv, fetchImpl }), store, db, keyPair, testEnv };[m
 }[m
[36m@@ -48,16 +48,30 @@[m [mtest('Discord interactions reject invalid signatures and accept a valid PING', a[m
   assert.deepEqual(valid.body, { type: 1 });[m
 });[m
 [m
[31m-test('feedback button callbacks are persisted after signature verification', async () => {[m
[32m+[m[32mtest('feedback button callbacks are persisted after deferred acknowledgement', async () => {[m
   const { app, db, keyPair } = appWithKeyPair();[m
[31m-  const body = { type: 3, member: { user: { id: 'feedback-user' } }, message: { id: 'msg-1' }, data: { custom_id: 'feedback:helpful' } };[m
[32m+[m[32m  const body = { type: 3, token: 'feedback-token', member: { user: { id: 'feedback-user' } }, message: { id: 'msg-1' }, data: { custom_id: 'feedback:helpful' } };[m
   const signed = signedBody(body, keyPair);[m
   const response = await request(app).post('/interactions').set('content-type', 'application/json').set('x-signature-ed25519', signed.sig).set('x-signature-timestamp', signed.timestamp).send(signed.raw);[m
   assert.equal(response.status, 200);[m
[31m-  assert.match(response.body.data.content, /Thanks/);[m
[32m+[m[32m  assert.deepEqual(response.body, { type: 5 });[m
   assert.equal(db.prepare('SELECT COUNT(*) AS c FROM feedback WHERE rating=?').get('helpful').c, 1);[m
 });[m
 [m
[32m+[m[32mtest('interaction endpoint sends type 5 before slow command work completes', async () => {[m
[32m+[m[32m  const { app, store, keyPair } = appWithKeyPair();[m
[32m+[m[32m  const originalListAiCredentials = store.listAiCredentials.bind(store);[m
[32m+[m[32m  store.listAiCredentials = async (...args) => { await new Promise((resolve) => setTimeout(resolve, 250)); return originalListAiCredentials(...args); };[m
[32m+[m[32m  const body = { type: 2, token: 'slow-command-token', member: { user: { id: 'slow-user' } }, data: { name: 'models', options: [] } };[m
[32m+[m[32m  const signed = signedBody(body, keyPair);[m
[32m+[m[32m  const startedAt = Date.now();[m
[32m+[m[32m  const response = await request(app).post('/interactions').set('content-type', 'application/json').set('x-signature-ed25519', signed.sig).set('x-signature-timestamp', signed.timestamp).send(signed.raw);[m
[32m+[m[32m  const elapsed = Date.now() - startedAt;[m
[32m+[m[32m  assert.equal(response.status, 200);[m
[32m+[m[32m  assert.deepEqual(response.body, { type: 5 });[m
[32m+[m[32m  assert.ok(elapsed < 200, `initial acknowledgement took ${elapsed}ms`);[m
[32m+[m[32m});[m
[32m+[m
 test('OAuth state is signed and tampering is rejected', () => {[m
   const state = createSignedState(env.SESSION_SECRET, 'discord-user');[m
   assert.equal(verifySignedState(env.SESSION_SECRET, state), 'discord-user');[m
[36m@@ -75,8 +89,7 @@[m [mtest('set-ai-key stores an encrypted value and does not echo the secret', async[m
   const signed = signedBody(body, keyPair);[m
   const response = await request(app).post('/interactions').set('content-type', 'application/json').set('x-signature-ed25519', signed.sig).set('x-signature-timestamp', signed.timestamp).send(signed.raw);[m
   assert.equal(response.status, 200);[m
[31m-  assert.match(response.body.data.content, /saved (encrypted|securely)/);[m
[31m-  assert.doesNotMatch(response.body.data.content, /sk-test/);[m
[32m+[m[32m  assert.deepEqual(response.body, { type: 5 });[m
   const credentials = store.listAiCredentials('u1');[m
   assert.equal(credentials.length, 1);[m
   assert.notEqual(credentials[0].encryptedApiKey, 'sk-test-123456789');[m
